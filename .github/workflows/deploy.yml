name: Deploy to Cloudflare Pages

on:
  push:
    branches:
      - main
      - master
  pull_request:
    branches:
      - main
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: Deploy
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Create Cloudflare Pages Project (if not exists)
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: |
          echo "🔍 检查项目是否存在..."
          
          # 检查项目是否存在
          RESPONSE=$(curl -s -X GET "https://api.cloudflare.com/client/v4/accounts/$CLOUDFLARE_ACCOUNT_ID/pages/projects/newtab-navigation" \
            -H "Authorization: Bearer $CLOUDFLARE_API_TOKEN" \
            -H "Content-Type: application/json")
          
          # 解析响应，检查是否成功
          if echo "$RESPONSE" | grep -q '"success":true'; then
            echo "✅ 项目已存在"
          else
            echo "📦 项目不存在，正在创建..."
            
            # 创建项目
            CREATE_RESPONSE=$(curl -s -X POST "https://api.cloudflare.com/client/v4/accounts/$CLOUDFLARE_ACCOUNT_ID/pages/projects" \
              -H "Authorization: Bearer $CLOUDFLARE_API_TOKEN" \
              -H "Content-Type: application/json" \
              -d '{
                "name": "newtab-navigation",
                "production_branch": "main"
              }')
            
            if echo "$CREATE_RESPONSE" | grep -q '"success":true'; then
              echo "✅ 项目创建成功"
            elif echo "$CREATE_RESPONSE" | grep -q "already exists"; then
              echo "✅ 项目已存在（可能在创建过程中已创建）"
            else
              echo "⚠️ 创建项目时可能遇到问题，但继续部署..."
              echo "响应: $CREATE_RESPONSE"
            fi
          fi

      - name: Check Secrets
        run: |
          echo "🔍 检查 Secrets 配置..."
          echo "CLOUDFLARE_API_TOKEN 是否存在: $([ -n "${{ secrets.CLOUDFLARE_API_TOKEN }}" ] && echo '是' || echo '否')"
          echo "CLOUDFLARE_ACCOUNT_ID 是否存在: $([ -n "${{ secrets.CLOUDFLARE_ACCOUNT_ID }}" ] && echo '是' || echo '否')"
          
          # 检查 API Token
          if [ -z "${{ secrets.CLOUDFLARE_API_TOKEN }}" ] || [ "${{ secrets.CLOUDFLARE_API_TOKEN }}" == "" ]; then
            echo "❌ 错误: CLOUDFLARE_API_TOKEN 未配置或为空"
            echo ""
            echo "请确认："
            echo "1. 访问 https://github.com/nianshu2022/newtab/settings/secrets/actions"
            echo "2. 检查是否存在名为 'CLOUDFLARE_API_TOKEN' 的 Secret（注意大小写）"
            echo "3. 如果不存在，点击 'New repository secret' 创建"
            echo "4. 如果已存在，检查值是否为空"
            exit 1
          fi
          
          # 检查 Account ID
          if [ -z "${{ secrets.CLOUDFLARE_ACCOUNT_ID }}" ] || [ "${{ secrets.CLOUDFLARE_ACCOUNT_ID }}" == "" ]; then
            echo "❌ 错误: CLOUDFLARE_ACCOUNT_ID 未配置或为空"
            echo ""
            echo "请确认："
            echo "1. 访问 https://github.com/nianshu2022/newtab/settings/secrets/actions"
            echo "2. 检查是否存在名为 'CLOUDFLARE_ACCOUNT_ID' 的 Secret（注意大小写）"
            echo "3. Secret 名称必须完全一致：CLOUDFLARE_ACCOUNT_ID"
            echo "4. 如果不存在，点击 'New repository secret' 创建"
            echo "5. 如果已存在，检查值是否为空"
            exit 1
          fi
          
          echo "✓ Secrets 配置正确"

      - name: Deploy to Cloudflare Pages
        uses: cloudflare/pages-action@v1
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          projectName: newtab-navigation
          directory: dist
          gitHubToken: ${{ secrets.GITHUB_TOKEN }}
          wranglerVersion: '3'

