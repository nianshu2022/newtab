{
  "name": "newtab-navigation",
  "version": "1.0.0",
  "description": "功能丰富的网站导航网站",
  "main": "index.js",
  "scripts": {
    "build": "tsc && npm run build:assets",
    "build:assets": "npm run copy:html && npm run copy:css",
    "copy:html": "node scripts/copy-files.js",
    "copy:css": "node scripts/copy-css.js",
    "dev": "npm run build && npm run preview",
    "preview": "serve dist -l 3000 -s"
  },
  "keywords": [
    "navigation",
    "newtab"
  ],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "devDependencies": {
    "serve": "^14.2.1",
    "typescript": "^5.3.3"
  }
}
